import Image from "next/image";
import { supabase, supabaseAdmin } from "@/lib/supabase"
export default function Home() {
  const setNewView = async () => {
  }
  setNewView();
  return (
    <div> server is running
    </div>
  );
}
